# Default paths — copy to .env.local and override as needed
SPARK_HOME       ?= $(REPO_ROOT)/../spark
ICEBERG_HOME     ?= $(REPO_ROOT)/../iceberg
SPARK_IMAGE      ?= localhost/spark-dev
SPARK_TAG        ?= latest
KIND_CLUSTER     ?= kind-cluster
MINIO_NAMESPACE  ?= minio
POLARIS_NAMESPACE?= polaris
SPARK_NAMESPACE  ?= spark
